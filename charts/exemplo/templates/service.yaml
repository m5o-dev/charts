apiVersion: v1
kind: Service
metadata:
  name: {{ include "exemplo.fullname" . }}
  labels:
    {{- include "exemplo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "exemplo.selectorLabels" . | nindent 4 }} 